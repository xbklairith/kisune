---
description: Create new feature specification
---

Use the Skill tool to invoke: `dev-workflow:spec-driven-planning`

Pass the feature name from $ARGUMENTS to Phase 1 (Feature Creation).

If no arguments provided, prompt user for feature name.
