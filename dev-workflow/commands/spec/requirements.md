---
description: Define requirements using EARS format
---

Use the Skill tool to invoke: `dev-workflow:spec-driven-planning`

Start at Phase 2 (Requirements Definition) with EARS format.

Feature context from $ARGUMENTS or use most recent feature.
